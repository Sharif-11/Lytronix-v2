# Loaded automatically by `vite build`. The value is baked into the bundle,
# so set the REAL public API base URL (including the /api path) before building.
VITE_API_URL=https://lytronix-v2.onrender.com/api
